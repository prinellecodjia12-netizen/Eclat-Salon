server/
storage/
dist/**/*.map
node_modules/
README.md

# Keep `script/` (build script) and TypeScript sources so Vercel can run `npm run build`
# Ignoring TS/TSX files prevented the build step from finding `script/build.ts` and client sources.
